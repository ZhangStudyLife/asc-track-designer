# ASC赛道设计器 - 项目说明

## 📁 项目结构

```
asc-track-designer/
├── src/              # React 源代码
├── public/           # 静态资源
├── electron.js       # Electron 主进程
├── package.json      # 项目配置
├── next.config.js    # Next.js 配置
└── dist/             # 打包输出目录
```

## 🚀 开发与打包

### 开发模式
```bash
# 双击运行
启动ASC赛道设计器.bat

# 或命令行
npm run dev
```

### 打包 EXE
```bash
# 双击运行
打包成EXE.bat

# 或命令行  
npm run dist:win
```

## 📦 打包输出

打包完成后在 `dist/` 目录：
- `ASC赛道设计器 2.0.0.exe` - 便携版（无需安装）
- `ASC赛道设计器 Setup 2.0.0.exe` - 安装版

## 🔧 核心文件

### electron.js
主进程文件，负责：
- 创建窗口
- 修复静态资源路径（`/_next/` → `./_next/`）
- 处理打包后的文件加载

### package.json
关键配置：
- `build.asar: true` - 启用 ASAR 压缩
- `build.files` - 指定打包文件
- `build.win` - Windows 平台配置

### next.config.js  
Next.js 配置：
- `output: 'export'` - 静态导出模式
- `images.unoptimized` - 禁用图片优化

## ⚠️ 常见问题

### 1. 打包后无法启动
**原因**：修改代码后没有重新打包
**解决**：删除 `dist/` 目录，重新运行打包

### 2. 交互功能不工作
**原因**：静态资源路径问题
**已解决**：electron.js 会自动修复路径

### 3. 开发者工具
打包版本会自动打开开发者工具（用于调试）
位置：electron.js 第 32 行

## 🧹 清理命令

```bash
# 清理构建文件
.\清理项目.bat

# 或手动删除
Remove-Item .next, out, dist -Recurse -Force
```
